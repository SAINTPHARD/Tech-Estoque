/**
package com.cadastro.demo.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.cadastro.demo.entity.Tech;

// Interface responsável por acessar o banco
// JpaRepository já fornece vários métodos prontos
public interface TechRepository extends JpaRepository<Tech, Long> {

	// Exemplos de métodos que já existem automaticamente:

	// save() -> salva um objeto
	// findAll() -> retorna todos os registros
	// findById() -> busca por ID
	// delete() -> remove um registro
}
*/